USAGE

  fez mod -c <module>

  fez module [--class=False] <module>

Attempts to create a module in the current dist and update the meta. Providing
`--class` causes this command to create a `unit class` rather than `unit module`.
Do NOT include 'lib/' in the path, eg

fez mod New::Module

Will create the file: `lib/New/Module.rakumod`.
